% dat2mat
% extract persistent homology barcodes from data matrix

% Author:       Baihan Lin
% Affliation:   Rabadan Lab, Columbia University
% Date:         04/2018

function [graph, bar] = mat2phb(dat, h, dist)

N = size(dat, 2);
graph = zeros(N,N);
if strcmp(dist, 'Euclidean')
    for x = 1:N
        for y = 1:N
            graph(x,y) = sqrt(sum((dat(:,x) - dat(:,y)).^2))
            graph(y,x) = graph(x,y);
        end
    end
end

switch h
    case 0
    
    otherwise
        disp(')
end